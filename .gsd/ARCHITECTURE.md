# Architecture

> Auto-generated by /map on 2026-09-04

## Overview

GDGC (Google Developer Groups on Campus) Website is a monorepo containing a modern client-side single-page application (SPA) built with React 19, Vite, Tailwind CSS v4, Framer Motion, and GSAP, paired with a lightweight Node.js/Express backend API scaffold.

```
┌─────────────────────────────────────────────────────────┐
│              Frontend (Vite + React 19)                 │
│  - React Router DOM v7 (10 Routes)                      │
│  - GSAP & Framer Motion Animations                      │
│  - Tailwind CSS v4 Styling & Dark Mode                  │
│  - Static Data Layer (data/*.js)                        │
├─────────────────────────────────────────────────────────┤
│            Integration (Currently Decoupled)            │
│  - No active API calls from frontend to backend yet     │
├─────────────────────────────────────────────────────────┤
│             Backend (Node.js + Express 4)               │
│  - Express REST API (/api/health)                       │
│  - CORS & dotenv configured                             │
│  - Scalable for event registration, contact forms, etc. │
└─────────────────────────────────────────────────────────┘
```

## Structure & Workspaces

The root repository uses npm workspaces (`workspaces: ["frontend", "backend"]`):

- **Root (`package.json`)**: Orchestrates commands (`npm run dev`, `npm run build`, `npm run backend:dev`) with `concurrently`.
- **`frontend/`**: Complete client-side application.
- **`backend/`**: Node.js/Express server.

## Components

### Frontend (`frontend/src/`)

#### 1. Routing & Layout
- **Location:** [App.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/App.jsx), [Layout.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/components/layout/Layout.jsx)
- **Features:** React Router DOM v7 browser router with `ScrollToTop` hook on route change. Common shell with responsive `Navbar` and `Footer`.

#### 2. Pages
- **[Home.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/Home.jsx)**: Hero section with dynamic orb animation, GDG interactive branding, stats counter, upcoming events spotlight, domain highlights, and interactive "BLACKOUT" clue hunt modal.
- **[About.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/About.jsx)**: Chapter mission, history, core pillars, and values.
- **[Events.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/Events.jsx)**, **[UpcomingEvents.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/UpcomingEvents.jsx)**, **[PastEvents.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/PastEvents.jsx)**: Event showcases, category filters, past recap cards.
- **[Team.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/Team.jsx)**: Core team members, domain leads, social links.
- **[Domains.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/Domains.jsx)**: Technical tracks (Web Dev, AI/ML, Cloud, Android, CyberSec, Design, etc.).
- **[Achievements.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/Achievements.jsx)**: Hackathon trophies, Solution Challenge milestones, community badges.
- **[Resources.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/Resources.jsx)**: Roadmaps, guides, learning resources categorized by domain.
- **[Contact.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/pages/Contact.jsx)**: Contact form and community social links.

#### 3. UI Components & System
- **Location:** `frontend/src/components/ui/`
- **Primitives:** `Button.jsx`, `Card.jsx`, `Badge.jsx`, `Input.jsx`, `GdgText.jsx` (Google themed colored typography), `InteractiveLogo.jsx`.
- **Special Components:** `BlackoutMysteryGame.jsx` (gamified clue hunt overlay), `AnimatedBackground.jsx` (particles/orbs).

#### 4. State & Context
- **[ThemeContext.jsx](file:///c:/Users/DIYA/gdgc-newWebsite/frontend/src/context/ThemeContext.jsx)**: Theme management (`light` / `dark`), synchronizing `classList` on document element and `localStorage`.

#### 5. Data Layer
- **Location:** `frontend/src/data/`
- Hardcoded JavaScript objects providing rich mock data (`events.js`, `team.js`, `domains.js`, `resources.js`, `achievements.js`).

### Backend (`backend/src/`)

- **Location:** [backend/src/index.js](file:///c:/Users/DIYA/gdgc-newWebsite/backend/src/index.js)
- **Role:** Express 4 server providing:
  - `GET /api/health` — Health check endpoint.
  - JSON body parsing (`express.json()`).
  - CORS middleware enabled.

## Data Flow

1. **Current State:** The frontend renders entirely from static data in `frontend/src/data/*.js`. User interaction triggers UI state changes, theme transitions, animations, and modal toggles locally in React state.
2. **Target State:** Dynamic events, member registrations, contact form inquiries, and resources will flow from frontend `fetch()` / `axios` calls to backend Express endpoints, persisting in a database.

## Integration Points

| Service / Endpoint | Type | Purpose | Current Status |
|--------------------|------|---------|----------------|
| `GET /api/health` | REST API | Server health monitor | Implemented |
| Contact Submission | REST API | Contact form messages | Pending integration |
| Event Registration | REST API | RSVP & ticket tracking | Pending integration |

## Technical Debt & Gaps

- [ ] **Dependencies Not Installed:** `node_modules` not installed at workspace root or subpackages.
- [ ] **Decoupled Backend:** Backend has only `/api/health`; frontend does not connect to backend API yet.
- [ ] **Static Data:** Event, team, domain, and resource data are hardcoded in static JS modules.
- [ ] **Database / Persistence:** Backend does not yet have a database (e.g. MongoDB, PostgreSQL, or Cloud Firestore).
- [ ] **Test Coverage:** No automated unit tests or integration tests configured yet.
- [ ] **Environment Configuration:** `backend/.env.example` exists, but active `.env` configuration and frontend Vite environment variables (`VITE_API_URL`) are pending.

## Conventions

- **Naming:** React components use PascalCase (`Home.jsx`, `ThemeContext.jsx`); utilities and data use camelCase (`cn.js`, `events.js`).
- **Styling:** Tailwind CSS v4 utility classes paired with `clsx` and `tailwind-merge` (`cn` helper).
- **Animations:** Hybrid use of Framer Motion for component enter/exit animations and GSAP ScrollTrigger via custom hooks (`useScrollAnimation.js`).
